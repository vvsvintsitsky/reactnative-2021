import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    padding: 5,
    paddingBottom: 10,
    aspectRatio: 1,
  },
  imageContainer: {
    width: '100%',
    alignItems: 'center',
  },
  image: {
    marginHorizontal: 'auto',
    aspectRatio: 1,
    width: 100,
  },
  name: {
    marginBottom: 10,
    marginTop: 5,
  },
});
