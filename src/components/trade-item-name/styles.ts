import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    color: '#4A4A4A',
    fontFamily: 'Roboto',
    fontWeight: 'normal',
    fontStyle: 'normal',
    fontSize: 15,
    lineHeight: 20,
  },
});
